import { layer } from "../map/map";

// Создаем массив направлений для проверки соседей
const directions = [
    { row: -1, col: 0 },
    { row: 1, col: 0 },
    { row: 0, col: -1 },
    { row: 0, col: 1 }
];

export const updatePotentials = () => {

};